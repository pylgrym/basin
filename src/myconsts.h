#pragma once

/* jg, fixme - i should split up, so that 
responsibility 'transp-color is diff. from colornone'?
(ie uses should name, what resp they need.)
*/
extern const COLORREF colorNone;// beware, this is actually transparency color!
extern const COLORREF colorTransp;// beware, this is actually transparency color!

class myconsts
{
public:
  myconsts();
  ~myconsts();
};

